import 'package:flutter/material.dart';

const primaryColor = Color.fromRGBO(0, 165, 181, 1);
const backgroudColor = Color.fromRGBO(12, 30, 44, 1);
const errorBackgroundColor = Color.fromRGBO(67, 38, 60, 1);
const secoundaryColor = Color.fromRGBO(249, 74, 126, 1);
const errorColor = Color.fromRGBO(249, 2, 77, 1);
const surfaceColor = Color.fromRGBO(63, 65, 67, 1);
const onSurfaceColor = Color.fromRGBO(255, 255, 255, 1);

const gradientsPrimaryColor =
    LinearGradient(colors: [secoundaryColor, primaryColor], stops: [0.35, 1.0]);
const gradientsPrimaryColor2 =
    LinearGradient(colors: [secoundaryColor, primaryColor], stops: [0.05, 1.0]);

const defaultPadding = 16.0;
